# Base URL of the Sea-Plus-Plus backend API.
# Used by src/api.js as the prefix for every /api/... fetch.
# In local dev, leave unset to fall back to http://localhost:8080.
# In production (Render), set this to the deployed backend URL.
VITE_API_BASE=https://sea-plus-plus-api.onrender.com
